<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/margin_medium"
    android:layout_marginBottom="@dimen/margin_medium"
    android:gravity="center_vertical"
    android:id="@+id/drawerItem"
    >

    <ImageView
        android:id="@+id/itemImage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="@string/app_icon_sample"
        app:layout_constraintBottom_toTopOf="@id/itemText"
        android:layout_gravity="top|center"
        android:src="@mipmap/code_icon_green"
        />

    <TextView
        android:id="@+id/itemText"
        android:textColor="@color/green"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="@dimen/list_item_name_text_size"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autoSizeTextType="uniform"
        android:layout_gravity="center|bottom"
        android:textAlignment="center"
        android:text="@string/app_name_sample"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>